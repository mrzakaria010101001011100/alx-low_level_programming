#include <stdio.h>

/**
 * entre point alphabet
 *
 * we can use putchar in output
 *
 * alwys RETURN 0 (success)
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
{
	putchar(ch);
	 h++;
}
	putchar('\n');
return (0);
}


