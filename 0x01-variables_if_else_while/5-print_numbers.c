#include <stdio.h>

/**
 * main -prints single digit numbers of base 10
 *
 * starting for 0.
 *
 * return: Always 0.
 */

int main(void)
{
	int n = 0;

	for (n < 10; n++)
		printf("%d", n);

	printf("\n");
	return (0);
}
